function load() {
    var header = document.querySelector('#header')
    var megaMenu = document.querySelector('#header-megamenu')

    // megaMenu.addEventListener('mouseover', function(event) {
    //     if (event.target.classList.contains('megamenu-section')){
    //         console.log(event.target.classList)
    //         if(!event.target.classList.contains('active')) {
    //             event.target.classList.add('active')
    //         }
    //     }
    // })
    // megaMenu.addEventListener('mouseout', function(event) {
    //     if (event.target.classList.contains('megamenu-section')){
    //         if(!event.target.classList.contains('active')) {
    //             event.target.classList.remove('active')
    //         }
    //     }
    // })
}

document.addEventListener('DOMContentLoaded', load)